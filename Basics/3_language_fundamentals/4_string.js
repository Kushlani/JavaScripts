/**
 * Any text is of string type 
 * string values must be with in quatations
 * string values are also called string literals (in programming langualges)
 * 
 */

var stringVariable = "<   This is a string value    >";
console.log(stringVariable);